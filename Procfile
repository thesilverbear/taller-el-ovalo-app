web: gunicorn run:app


# Le dice a Railway que para el proceso web (web:) ejecute el servidor gunicorn. 
# run:app significa que busca en el archivo run.py la variable llamada app (a.k.a la instancia de Flask creada por create_app()).